# Domain

قواعد وأنواع **خالصة من إطار العمل**: لا تستورد من `next/*` أو `prisma` هنا.

- `errors.ts` — أخطاء نطاق يمكن رفعها من طبقة التطبيق.
- أضف لاحقًا: كائنات قيمة (مثل `PhoneNumber`)، وسياسات تجميع الحضور، إلخ.
